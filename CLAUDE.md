# Claude Code Instructions

This repository is registered with the App Factory. Before editing, read
`.factory/repository-map.json`, `.factory/project-context.json`,
`.factory/standard-lock.json`, `docs/README.md`, and only the canonical
documents and feature contracts routed for the current task.

This is an existing project. Preserve its architecture, disclose checks that
did not run, and do not claim `done` while platform or human review remains.
Search `.factory/library-catalog.json` before implementing cross-cutting
infrastructure and record local reusable candidates in
`docs/REUSABLE_COMPONENTS.md`.
