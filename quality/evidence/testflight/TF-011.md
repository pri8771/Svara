# TF-011 — Signed Build Upload Evidence

- Date: 2026-07-30
- Status: `done`
- Binary source commit: `6e9d0b16de5713119770fa160292442dbe32baba`
- App Store Connect app: Svara, Apple ID `6785557134`
- Bundle: `com.primandir.svara`
- Version/build: 1.0 (2)
- Upload accepted: 2026-07-30 18:51 EDT
- Processed Apple state: `Ready to Submit` / binary `Validated`
- TestFlight build reference:
  `1193bd4d-d756-477c-840b-8fa0918fe25e`

## Preflight and archive

The source tree was clean at the binary commit. Factory registration,
repository checks, 153/153 automated tests, Release analysis, and archive
inspection passed. The fresh archive was
`/private/tmp/Svara-1.0-2.xcarchive`; it contained the expected app only and
matched the documented bundle/version/build/resources.

## Distribution and upload

The archive was exported with App Store Connect automatic/cloud-managed
signing for team `796XH483R4`. The export configuration preserved build number
2, uploaded symbols, and explicitly set
`testFlightInternalTestingOnly=false`.

Xcode completed App Store Connect analysis and SPI analysis, obtained upload
instructions, uploaded the package, and reported:

```text
Upload succeeded.
Uploaded Svara
** EXPORT SUCCEEDED **
```

App Store Connect TestFlight → iOS first displayed version 1.0, build (2),
created Jul 30, 2026 6:51 PM, status `Processing`. It subsequently moved into
the Version 1.0 build list as `Ready to Submit`, expiring in 90 days.

## Processed build inspection

The build detail reports:

- App Name Svara; Binary State `Validated`;
- bundle `com.primandir.svara`; version 1.0; build 2;
- iPhone device family, arm64, minimum iOS 17.0;
- symbols included; no on-demand resources;
- non-exempt encryption `No`;
- application identifier `796XH483R4.com.primandir.svara`;
- `get-task-allow=false` and `beta-reports-active=true`;
- no compliance prompt, processing error, or warning requiring action.

The build is ready for the owner’s TF-015 smoke. Close-friends invitations
remain blocked by TF-006–TF-008, TF-010, TF-013/TF-014, and TestFlight App
Review. The upload ran while TF-009 remote CI evidence was still pending; that
process exception does not change the binary, but TF-009 must still be closed
before treating repository release governance as complete.
